% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sahi-split.R
\name{prepare_sahi_split}
\alias{prepare_sahi_split}
\title{Prepare SAHI crop split parameters}
\usage{
prepare_sahi_split(x, size = c(512L, 512L), overlap_size_ratio = c(0.2, 0.2))
}
\arguments{
\item{x}{Object to determine image dimensions from. Can be:
\itemize{
\item A numeric vector of length 2: \code{c(height, width)}
\item A \code{torch_tensor}: dimensions extracted from the tensor shape
\item A \code{magick-image}: dimensions extracted from image metadata
\item A dataset (e.g. \code{coco_detection_dataset}): dimensions extracted from
the first sample's metadata or loaded image
}}

\item{size}{Integer vector of length 2 containing crop height and width
in the form \code{c(height, width)}.}

\item{overlap_size_ratio}{Numeric vector of length 2 containing vertical
and horizontal overlap ratios in the form
\code{c(overlap_height_ratio, overlap_width_ratio)}.}
}
\value{
An object of class \code{sahi_split} containing:
\describe{
\item{crop_windows}{List of crop windows with
each window is a list with \code{top}, \code{left}, \code{height} and \code{width}
fields specifying the region to crop from the original image.}
\item{size}{Integer vector of length 2: the crop \code{c(height, width)} used
to generate the split.}
\item{overlap_size_ratio}{Numeric vector of length 2: the vertical and
horizontal overlap ratios \code{c(overlap_h, overlap_w)} used.}
\item{image_height}{Height of the original image (integer).}
\item{image_width}{Width of the original image (integer).}
}
}
\description{
Precomputes crop windows for the Slicing Aided Hyper Inference (SAHI)
approach. The returned \code{sahi_split} object can be passed to
\code{transform_sahi_crop()} and \code{target_transform_sahi_crop()} so that
transforms remain type-invariant (returning the same type as their input).
}
\examples{
\dontrun{
# From a torch tensor
img_url <- "https://raw.githubusercontent.com/obss/sahi/main/demo/demo_data/small-vehicles1.jpeg"
img <- base_loader(img_url) \%>\% transform_to_tensor()
sp <- prepare_sahi_split(img, size = c(512, 512), overlap_size_ratio = c(0.2, 0.2))

crops <- transform_sahi_crop(img, sp)
grid <- vision_make_grid(crops, scale = TRUE, num_rows = 3)
tensor_image_browse(grid)

# From a dataset (SAHI as part of the transform pipeline)
ds <- coco_detection_dataset(train = FALSE, year = "2017", download = TRUE)
sp_ds <- prepare_sahi_split(ds, size = c(200, 200), overlap_size_ratio = c(0.2, 0.2))

ds <- coco_detection_dataset(train = FALSE, year = "2017",
  transform = . \%>\% transform_to_tensor() \%>\%
    transform_sahi_crop(sp_ds))

item <- ds[1]
grid <- vision_make_grid(item$x, scale = TRUE, num_rows = 3)
tensor_image_browse(grid)
}

}
\concept{sahi}
