% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target-transforms-detection.R
\name{target_transform_affine}
\alias{target_transform_affine}
\alias{target_transform_affine.object_detection_target}
\title{Apply an affine transformation to a detection target}
\usage{
target_transform_affine(
  target,
  angle = 0,
  translate = c(0, 0),
  scale = 1,
  shear = 0,
  center = NULL
)

\method{target_transform_affine}{object_detection_target}(
  target,
  angle = 0,
  translate = c(0, 0),
  scale = 1,
  shear = 0,
  center = NULL
)
}
\arguments{
\item{target}{An \code{object_detection_target}, as returned by the \code{.getitem()}
method of an \link[=coco_detection_dataset]{object detection dataset}, containing
at least:
\itemize{
\item \code{boxes} — tensor of shape \verb{(N, 4)} in xyxy format, or \verb{(N, 5)} in
xyxyr format for an already rotated target
\item \code{image_height} — original image height, defining the rotation centre
\item \code{image_width} — original image width, defining the rotation centre
\item Other fields (labels, etc.) are preserved unchanged
}}

\item{angle}{(numeric): Rotation angle value in degrees, counter-clockwise.}

\item{translate}{(integer vector of length 2): Horizontal and vertical
translations (post-rotation translation).}

\item{scale}{(numeric): Overall scale.}

\item{shear}{(numeric vector or numeric): Shear angle value in degrees
between \code{-180} to \code{180}, clockwise direction. If a numeric vector is
specified, the first value corresponds to a shear parallel to the x-axis,
while the second value corresponds to a shear parallel to the y-axis.}

\item{center}{(numeric vector of length 2, optional): Optional center of
rotation, \code{c(x, y)}. Default is image center.}
}
\value{
A list with the same structure as the input target, where \code{boxes} is a
tensor of shape \verb{(N, 5)} in xyxyr format.
}
\description{
Applies an affine transformation (rotation, translation, scale and shear) to
the bounding boxes of a detection target so that they follow an image
transformed by \code{\link[=transform_affine]{transform_affine()}} with the same parameters. Each box centre
is moved by the affine transformation around the given centre, the box is
scaled by \code{scale} and rotated by \code{angle}, and the result is returned as a
rotated box in xyxyr format. Passing an already rotated target accumulates the
rotation, so transforms compose.
}
\examples{
\dontrun{
target <- list(
  boxes = torch_tensor(matrix(c(10, 20, 50, 60), ncol = 4), dtype = torch_float32()),
  labels = torch_tensor(1L, dtype = torch_long()),
  image_height = 100L,
  image_width = 200L
)
class(target) <- c("object_detection_target", "list")
target_transform_affine(target, angle = 20, translate = c(10, 5), scale = 1.1, shear = 8)
}

}
\seealso{
Other target_transforms_detection:
\code{\link[=prepare_sahi_split]{prepare_sahi_split()}},
\code{\link[=target_transform_resize]{target_transform_resize()}},
\code{\link[=target_transform_rotate]{target_transform_rotate()}}
}
\concept{target_transforms_detection}
